# R9 / 0.4.3 验收报告（发布候选 · 本地收口）

**状态：发布候选完成（2026-08-13）。尚未 Tag / 尚未 GitHub Release / 尚未安装正式 worker。** 基线提交 `fdfa22a5d24dbd3719e44d30c4414fd9ac3042a6`；本候选在其上叠加长 Session 连续性收口与协议字段 whole-field fallback 窄修。活动 `dist/` 保留历史 0.4.2 四制品（零漂移）并新增 0.4.3 四制品。

## 产品目标

Credential Guard 0.4.3 将长 Session 连续性能力正式收成发布候选：累计大请求不再因总大小误阻断；历史 residual / scanner 可隔离则隔离后继续；动态键碰撞分配唯一安全 key；边界未知编码私钥在内容类字段整字段占位后继续；协议骨架字段（`model` / `role` / `name` / `tool_call_id`）fail-closed（Provider=0）。仅改 Provider-bound 副本，不改 canonical Session。

方案：`docs/R9-0.4.3-长Session连续性修复方案.md`。Release notes：`docs/R9-0.4.3-Release-Notes.md`。

## 候选身份

| 项 | 值 |
| --- | --- |
| 基线 commit | `fdfa22a5d24dbd3719e44d30c4414fd9ac3042a6` |
| `plugin.yaml` version | **0.4.3** |
| `candidate_manifest_sha256` | `0882b64ce99f32443443c98a5393de5c3971fa8b76e0586621b162df073a7e4d` |
| `SOURCE_DATE_EPOCH` | `1704067200` |
| `PYTHONHASHSEED` | `0` |
| `TZ` | `UTC` |
| 构建 Python | `/Users/yelei/.hermes/hermes-agent/venv/bin/python` |
| plugin ZIP 成员数 | **35** |

## 0.4.3 发布物（designated，双临时构建 copy2 实测）

| 种类 | 文件名 | SHA-256 |
| --- | --- | --- |
| plugin zip | `credential-guard-0.4.3-hermes-plugin.zip` | `738bc8ae4e1973a50efba604602a9fb3c7a6739efb95e48024b6a1975e97dacb` |
| wheel | `hermes_credential_guard-0.4.3-py3-none-any.whl` | `a7bf91e1ceb263fc2c1f468baac7c6990d6c2899c27409704e31fff13bb1752c` |
| sdist | `hermes_credential_guard-0.4.3.tar.gz` | `5f7b939ca37ff6ef38eaaaba381536cafdc0196a80b558705e7016997282033b` |
| versioned manifest | `artifact-manifest-0.4.3.json` | `86321bd8fc7ee4be994d3202f368bae114a23fd18bdea257eb5e810c96231127` |

大小（字节）：ZIP `111985`；wheel `113560`；sdist `96318`；manifest `812`。

## 双构建与旧 0.4.2 零漂移

- 两次独立 `tempfile.mkdtemp()` 调用生产 `build_all(tmp)`（禁止真实 `dist/` 作 out_dir；未调用 `main()`）。
- wheel / sdist / plugin ZIP / versioned manifest **逐字节 SHA-256 一致**后 `shutil.copy2` 落地。
- 落地后对 `artifact-manifest-0.4.3.json` 运行生产 `verify_artifact_manifest(...)`：**PASS**。
- 旧 0.4.2 四文件相对 `/tmp/cg_043_dist_before.txt` **零漂移**：

| 文件 | SHA-256 | 大小 |
| --- | --- | --- |
| `credential-guard-0.4.2-hermes-plugin.zip` | `95d96aa82f64701dfc0b5862ba3671feb98b7d5c07a61f350dbe65287fb60ccf` | 101656 |
| `hermes_credential_guard-0.4.2-py3-none-any.whl` | `7e514b6c47ea26867674e189df4f23b917d98f932e2832c2b57b4e4426942954` | 103229 |
| `hermes_credential_guard-0.4.2.tar.gz` | `97bd5fb3e5ef26efd0964598fedf6f7e3fc751b0bfa3d3c96ff1a425a5ee01d1` | 85902 |
| `artifact-manifest-0.4.2.json` | `51b4ed98dc244f19e00630c44f32e0c3bfd7c2090e2d979484c4721ac3490752` | 812 |

## 协议字段窄修（严格 TDD）

| 阶段 | 结果 |
| --- | --- |
| RED（生产未收窄） | `model` / `role` / `name` / `tool_call_id` 四参 + mutation：**5 failed**（期望 RED）；content/arguments：**2 passed** |
| GREEN | `tests/test_protocol_field_whole_field_fallback.py`：**7 passed** |
| 旧语义 mutation | 放宽 allowlist → 协议字段整字段继续（门禁承重） |

## 强制验证读数（本轮实测）

| 项 | 结果 |
| --- | --- |
| 协议字段专项 | **7 passed** |
| 今日核心连续性组合（long-session / unresolved / collision / residual / scanner / block-structure / protocol） | **142 passed** |
| 官方全量非构建 `scripts/run_r5_nobuild_pytest.py` | **1904 passed, 3 xfailed, 0 failed** |
| 既有 xfail（不得新增） | AC8、AC9、R5 provider-closure wire gap：**3 xfailed**（历史既有；无新增 skip/xfail） |
| release/package/manifest/composition/README/opt-in + designated hash | **96 passed**（含 composition / dist policy / reproducible_release / README / opt-in） |
| 隔离安装最终 ZIP `scripts/run_r9_043_final_zip_tests.py` | **3 passed**（PluginManager 装载；累计大请求/collision/unresolved/protocol/residual/scanner+trailing/final gate；source-fallback mutation 真实承重） |
| 临时安装路径 | `installed_from_zip=True`；模块 `__file__` 在临时插件目录，不在 repo 源码树 |
| source-fallback mutation（round2 收口） | **真实承重**：临时安装副本删除 `.../plugins/credential-guard/credential_guard`；repo 源码保持健康；neutral cwd + 干净 Hermes venv 子进程走 `PluginManager.discover_and_load(force=True)`；失败方式 `No module named 'credential_guard'`（fail-closed）；任一 `credential_guard*` `__file__` 不指向源码树。因素控制：人为把 repo 根放回优先 `sys.path` 时探针检出 source fallback（`detected_source_fallback=True`），证明判定非恒真。旧门禁仅 `prove_installed_module_path` 路径布尔、未损坏安装副本，属假绿，已替换。 |
| compileall + in-memory compile | **exit 0**（168 个 py 文件） |
| `git diff --check` | **exit 0** |
| 双构建字节一致 + 0.4.2 零漂移 | **PASS** |

## 成分审计（落地制品）

- plugin ZIP / wheel：无 `tests/`、`scripts/`、任务书、缓存、私钥头、`.pem/.key/id_rsa`、源码树路径回落；生产模块清单完整（`tests/test_r6_artifact_composition.py` 承重）。
- sdist：按项目声明 prune 策略审计（无 tests/scripts/docs 业务树）。
- plugin ZIP 成员数 35（相对 0.3.1=48 已下降，契约 `< 48`）。

## 版本字面量分类（摘要）

| 分类 | 示例 |
| --- | --- |
| 历史锚点保留 | `docs/R8-0.4.2-*`、`scripts/run_r8_042_*`、`dist/*0.4.2*`、R8 ZIP SHA、拓扑中的 0.4.2 条目 |
| 当前版本需改 | `plugin.yaml` / `pyproject.toml` / `PLUGIN_VERSION` / README 安装块 / designated mapping / dist policy |
| 新版本并存 | `dist/` 同时含 0.4.2 与 0.4.3 各四文件；历史 runner 不动，新建 `run_r9_043_*` |

## 未做（明确边界）

- 主代理独立验收
- `git commit` / `push`
- 创建 Tag `v0.4.3` / GitHub Release
- 正式 worker / 真实 Profile 安装与启用

## 独立复审

本文件为本地发布候选收口证据；两路独立只读复审与主代理签收待后续进行。不自称已 Tag / 已 Release。
