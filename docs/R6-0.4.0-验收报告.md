# R6 / 0.4.0 验收报告（当前 designated）

## 结论

**技术侧 PASS（0.4.0）—— R6 完成标准第 7 / 8 / 9 / 10 / 11 条已关闭；仅剩第 12 条（用户亲自在正式 worker 安装验收）**

第 10 条于 2026-08-06 按完成标准原文（**两路独立安全复审均 PASS**）达成：路 1 cursor-agent 综合复审
**PASS**、路 2 Hermes 安全边界复审 **PASS**，两路均针对同一冻结身份、各自独立复算并确认零改动。
第三路（证据真实性）经三轮实跑仍未取得 PASS 形式的结论，其性质与处置见「尚存边界」第 1 条 ——
**三轮复审在产品侧零缺陷，全部 BLOCKING 均为主 Agent 维护冻结证据时的文字笔误**，
经用户于 2026-08-06 拍板：不再阻塞第 12 条，记为行政类遗留。

| 完成标准 | 状态 | 证据 |
|---|---|---|
| 第 7 条：真实制品成分审计 + 0.4.0 落地 `dist/` | PASS（R6 slice 3） | `tests/test_r6_artifact_composition.py`；`dist/` 八成员 |
| 第 8 条：真构建 + 双构建字节一致 | PASS（R6 slice 2） | `tests/r6_real_build_check.py` / `scripts/run_r6_build_tests.py` |
| 第 9 条：已安装 ZIP 审批链 + 15 格 wire + manifest↔registry | PASS（R6 4a/4b） | `scripts/run_r6_installed_zip_tests.py`；`KNOWN_GAP_1` 已关 |
| 第 10 条：两路独立安全复审 PASS 且源码/报告/manifest/最终 ZIP 身份一致 | **PASS（按原文口径）** | 冻结侧车 `.r6-freeze-evidence.sha256` 锚定身份（当前值见其 `R6_TECHNICAL_CANDIDATE_SHA256`，历史链见 `IDENTITY_HISTORY`）。第 1 轮针对 291 文件 / `c1f7c91b…8766`：路 1 cursor-agent **PASS**（九方面齐全、零 BLOCKING）、路 2 Hermes 安全边界 **PASS**（1–8 条齐全、零 BLOCKING），两路各自独立复算身份得同值、`MATCHES_SIDECAR=True`、全程仓库零改动。第 3 路（证据真实性）跑了三轮：第 1 轮 429 限流未出结论；第 2 轮 BLOCKING×1；第 2b 轮 BLOCKING×2 —— **三轮所有 BLOCKING 均为主 Agent 冻结证据文字笔误，产品侧零缺陷**，详见「尚存边界」第 1 条 |
| 第 11 条：单文件模板 + 旧配置迁移路径验证 + 安装/验证/卸载/回滚指南 | PASS（slice 5） | 本报告 + `docs/R6-0.4.0-安装与运维指南.md` + 任务 C 实测 |
| 第 12 条：用户亲自在正式 worker 安装验收 | **待用户执行** | 见安装运维指南；Agent 不代操作正式 worker |

**本报告口径更正（2026-08-06 收尾复核）**：此前版本的完成标准表格**整行漏列第 10 条**，并据此在结论里写成「仅剩第 12 条」——那是口径缺失造成的误判，不是第 10 条曾经通过。真实剩余为**第 10 条 + 第 12 条**两项。

本刀未构建、未改 `dist/` 八成员、未修改正式 worker 任何文件。

## 威胁边界

```text
可信：本机 Hermes、Hermes 标准审批机制、Credential Guard、本地 credential-guard.json、本地执行器
不可信：外部模型、模型供应商、模型生成参数、Provider 返回内容
```

产品契约：审批前只分析逻辑引用，不解析本次执行真值；拒绝或超时则真值解析与下游执行均为 0；批准后才在受控本地执行边界复核并短暂注入。  
不宣称 OS 级 DLP。AC8/AC9 所述本机日志 / WAL / sessions 残余明文为已接受的可信区后续加固项。

## R6 TDD / 切片摘要（非 M3 编码轮）

| 切片 | 内容 | 结果 |
|---|---|---|
| 1 | 版本 0.4.0 + 绊线授权通道 + manifest 顶层键收缩 | PASS |
| 2 | 真构建 + 双构建字节一致（临时 out 目录；禁写 `dist/`） | PASS |
| 3 | 真实制品成分审计 + 0.4.0 落地 `dist/` | PASS |
| 4a | 0.4.0 安装包隔离环境审批链 | PASS |
| 4b | 15 格 wire 矩阵 + manifest↔registry；关闭 `KNOWN_GAP_1` | PASS |
| 5（本刀） | designated 验收报告 + 安装运维指南 + 真实迁移路径验证 | PASS（技术侧）；第 12 条留给用户 |

## 当前验收命令结果（0.4.0）

**当前有效读数（2026-08-06 第 10 条复审冻结后；本节下方 slice 5 读数已被推翻，保留作过程记录）**

| 命令 | 结果 |
|---|---|
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/run_r5_nobuild_pytest.py -q` | **1587 passed, 3 xfailed, 0 failed**（173.87s，第 2b 轮身份） |
| 五门禁（同下方命令行） | **319 passed, 0 failed**（35.08s，第 2b 轮身份） |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/audit_legacy_residue.py` | **39 项**（26 dual_file_runtime + 13 unresolved_dynamic_sink），CLI exit 1（设计如此）；权威判据 `tests/test_legacy_residue_gate.py` **73 passed** |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/run_r6_installed_zip_tests.py -q` | **35 passed, 0 failed**（25.29s） |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/run_r6_build_tests.py -q` | **3 passed**（1.94s）；跑完 `dist/` 八成员逐字节零漂移 |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m compileall -q credential_guard scripts tests` | **exit 0** |

读数沿革：**每份复审任务书本身都是一条已登记的台账路径**，在
`test_mutation_omit_each_added_path_calls_formal_predicate` 里各贡献 1 个参数化用例，
所以每加一份任务书，全量与五门禁各 +1。

```text
第 1 轮   291 文件 / c1f7c91b…   1585 / 317   三路独立复跑 179.21s / 182.66s / 187.27s 全一致
第 2 轮   292 文件 / f204b64f…   1586 / 318   + .r6-round2-evidence-authenticity-task.md
                                              第 2 轮复审独立复跑 174.06s / 39.75s 一致
第 2b 轮  293 文件               1587 / 319   + .r6-round2b-blocking-fix-verify-task.md
```

第 2b 轮的身份哈希不在此处写死 —— 以 `.r6-freeze-evidence.sha256` 的
`R6_TECHNICAL_CANDIDATE_SHA256` 为唯一真值，历史链见该侧车的 `IDENTITY_HISTORY`。
（本轮 B2 的成因正是在散文里嵌了「当前身份」的哈希字面量，改一次 SHA 就过期。）

第 2 轮复审已用 `--collect-only` 独立核实「+1 恰好来自新任务书自身」这一说法。
**两项读数必须同批更新** —— 第 2 轮的 BLOCKING 正是只改了五门禁、漏改全量造成的。

### 台账遗漏与修复（slice 5 读数为何被推翻）

slice 5 的读数在 **17:29** 采集，当时确为全绿。**17:42** 落地的 `.r6-progress-html-backfill-task.md`（进度 HTML 回填委派简报）未登记进拓扑台账，此后全量转红且无人复跑：

```text
修复前实测   2 failed, 1580 passed, 3 xfailed
  FAILED tests/test_r5_topology_gate.py::test_live_workspace_uses_phase_detector_and_classify
         → unclassified live paths: ['.r6-progress-html-backfill-task.md']
  FAILED tests/test_r3c_historical_identity_gate.py::test_r3c_reclosure_identity_is_layered_from_r4_workspace
         → assert 292 == ((271 + 70) - 50)
```

修复方式：把该文件登记进两个台账（`R5_ADDED_PATHS` 与 `_R5_PLANNING_DELTA_PATHS`），与前七个 slice 简报同一处理方式。**不删文件**——本项目非 Git 仓库，删除不可逆；登记是纯追加、可回退。

**门禁未被放宽的 negative 证明**：造一个未登记的 `.r6-negative-probe-unclassified.md`，两条断言同时转 RED、退出码 1；删除探针后恢复绿。计数变化 314→315 / 1580→1583 全部来自新登记路径在 omit-mutation 参数化集合里生成的用例，说明新条目真进了 mutation 覆盖，不是死条目。

第 10 条复审阶段又用**参数注入**方式（不往仓库写文件）把这个证明加强到六例，全部实测：

```text
control  健康 live 集合                    errors=0  GREEN
neg1     塞入未登记文件                     errors=2  RED
neg2     从台账撤销 backfill 那条登记        errors=2  RED   ← 登记是承重的
neg3     从台账撤销侧车那条登记              errors=2  RED
neg4     已声明新增的文件在 live 中缺失      errors=2  RED
neg5     已声明删除的旧模块在 live 中复活    errors=2  RED
台账基数  R5_ADDED 62 / DELETED 25 / PREFIXES 2 / MODIFIED 44，全部非空
```

`neg2` 是判据核心：若那次登记属于「放宽门禁」，撤掉它应当仍为绿；它转红，说明门禁在真判、登记只是精确补齐分类。台账基数全部非空，排除「参数化退化成空集造 vacuous 绿」。

教训（已并入判据）：**验收报告里的绿只对采集那一刻成立**；此后任何新增文件都必须复跑全量，报告读数不能当作现时状态引用。

相对 slice 5 开工基线 **1577 passed / 5 xfailed / 0 failed** 的历史归因（slice 5 当时口径）：

- **+2 passed / −2 xfailed**：翻掉 `test_k2_docs_final_plugin_zip_hash_matches_manifest_when_present` 与 `test_k2_wrong_current_report_hash_must_fail`（designated 报告 + `_DESIGNATED_REPORTS["0.4.0"]`）。
- **+3 passed**：`R5_ADDED_PATHS` 增 3 条路径带来的 topology omit 参数化用例（任务文件 + 两份交付文档）。
- 剩余 **3 xfailed**（未翻）：`test_r5_provider_live_wire_decoy_count_zero`、`test_ac8_agent_log_must_be_zero_plaintext`、`test_ac9_state_and_sessions_must_be_zero_plaintext`。

五门禁相对基线 **311 → 314**：同上 +3 `R5_ADDED_PATHS` 参数化。

收工后：`dist/` 仍 8 成员、字节与开工快照一致；无 `build/`；无 `*.egg-info`。

## 0.4.0 发布物（当前 designated）

| artifact | path | sha256 |
|---|---|---|
| wheel | `dist/hermes_credential_guard-0.4.0-py3-none-any.whl` | `6c457db3641eabee532a380cf0135308dd4ac846c6926b65b00bbcee723b34cf` |
| sdist | `dist/hermes_credential_guard-0.4.0.tar.gz` | `a7839c48d2f02550c4cfb95f394468783eee5ba18a301edc4e5f571dea9f987e` |
| plugin zip | `dist/credential-guard-0.4.0-hermes-plugin.zip` | `1fbc8c38da81226ef8a98f50702f2b3f5b369c5ce4767b8d0de8b2aaad20908d` |
| versioned manifest | `dist/artifact-manifest-0.4.0.json` | `be6f2e320cfff32abb81a0bf13701fc1a4e2fec93665f871c0c7c4a972724c7a` |

- 现行版本化 manifest 顶层 6 键（R6 slice 1 收缩后）：`version` / `build` / `candidate_manifest_sha256` / `plugin_zip` / `wheel` / `sdist`
- `candidate_manifest_sha256=f04ece15258588288f6360fd9bcc10fe4dc246a222e647e6127c916c2fdc8611`（摘自 `artifact-manifest-0.4.0.json`）
- `SOURCE_DATE_EPOCH=1704067200`，`PYTHONHASHSEED=0`，`TZ=UTC`，归档归一化（构建记录见 slice 2/3 证据）
- 历史 `dist/artifact-manifest.json` 仍为 **0.3.1** 冻结指针，本刀未改

## 任务 C：真实旧配置迁移验证（禁 1 边界内）

### 读了哪些键名 / 未读任何值

| 来源 | 读取方式 | 键名层级（值未读） |
|---|---|---|
| `$WORKER_HOME/credential-guard/credential-guard.json` | JSON 键扫描；叶子仅记录类型与长度 | 顶层：`version`, `credentials`, `bindings`；credential 项键：`type`, `value`；binding 项键：`type`, `credential_ref`, `target`, `inject`, `approval`；`target` 子键：`scheme`, `host`, `port`；`inject` 子键：`type`, `location` |
| `$HOME/.hermes/credential-guard/credentials.json` + `targets.json`（默认 HERMES 店，非 worker） | 同上，仅作机器上唯一真实双文件对 | credentials 顶层：`version`, `credentials`；项键：`type`, `username`, `password`；targets 顶层：`version`, `targets`；项键：`type`, `host`, `port`, `database`, `credential_ref` |
| worker `plugins/credential-guard/plugin.yaml` | 仅键名与 `version`/`name` 行 | `name`, `version`, `description`, `provides_hooks`, `provides_middleware`, `provides_tools` |

**明确声明：未读取任何凭证值；未读取 `~/.hermes/profiles/worker/config.yaml`；未触碰 `~/.ssh/`；未在 worker profile 内就地迁移；未安装/启用/重启正式 worker。**

条目名字在报告中一律以 `<cred#N>` / `<bind#N>` / `<name:len>` 占位，不转写可能含主机信息的字面量。

### 迁移结果（0.4.0 已安装 ZIP，临时副本）

1. **Worker 店并非双文件**：`$WORKER_HOME/credential-guard/` 已是单文件 `credential-guard.json`（无 `credentials.json` / `targets.json`）。因此「worker 双文件 → 单文件」路径在现状下不适用。
2. **Worker 现有单文件在 0.4.0 下加载失败**：缺 HTTP binding 必填块 `request`（`allowed_methods` / `allowed_paths`）→ `CONFIG_SCHEMA`。在临时副本上补上合成 `request` 后加载成功。安装指南已写明必须补全。
3. **机器上真实双文件对**（`$HOME/.hermes/credential-guard/`，canary 遗留）拷贝到 `mkdtemp` 后，用 `installed_zip_plugin.py` 安装的 0.4.0 调用 `migrate_config`：  
   **`MIGRATION_REQUIRES_MANUAL_REVIEW`**（按设计；非空 v1 永不静默写成空 v2）。未生成 `credential-guard.json`；副本中旧双文件保持 0600。源目录 mtime/size 与操作前一致。
4. **空双文件正例**（同 0.4.0 安装树）：`migrate_config` 成功，`credential-guard.json` 顶层键 `version`/`credentials`/`bindings`，两者皆空；旧文件变为 `*.v1.bak`。

### worker profile 元数据未变

操作前后对  
`$HOME/.hermes/profiles/worker/plugins/credential-guard` +  
`$HOME/.hermes/profiles/worker/credential-guard`  
全部普通文件做 `path|size|mtime` 快照 diff：**无差异**。

## worker / Hermes 未变化证据

- 本刀未安装、未升级、未启用/禁用 worker 插件，未重启 gateway。
- 未读 `config.yaml`，未读 `~/.ssh`。
- Hermes 源码不在本仓库；本刀未改 Hermes。

## 尚存边界

1. **第 10 条：按原文达成（两路 PASS），第三路结论缺失属已接受的行政遗留** —— 完成标准原文要求「两路独立安全复审均 PASS 且源码/报告/manifest/最终 ZIP 身份一致」；本项目里程碑签收惯例为三路（Cursor + 两路 Hermes）。

   **达成部分**：路 1 cursor-agent 综合复审 **PASS**（九方面齐全、零 BLOCKING）、路 2 Hermes 安全边界复审 **PASS**（1–8 条齐全、零 BLOCKING），两路均绑定第 1 轮身份 291 文件 / `c1f7c91b…8766`，各自独立复算得同值、确认全程零改动。原文口径已满足。

   **缺失部分**：第 3 路（证据真实性 / 假绿检测）跑了三轮，均未取得 PASS 形式的结论：

   ```text
   第 1 轮  Hermes 子代理     620s 时 HTTP 429 上游限流 + 迭代预算耗尽，无结论
                             七项必查在进程内实际跑完，只是未汇总
   第 2 轮  cursor-agent      BLOCKING × 1  侧车 FULL_NOBUILD_PYTEST 停在 1585，
                             同身份实测 1586（主 Agent 更新五门禁时漏同步全量）
   第 2b 轮 cursor-agent      BLOCKING × 2  B2：散文里写死了会过期的身份哈希字面量
                             B3：ROUND_2_IDENTITY_NOTE 用现在时描述已过期的轮次
   ```

   **关键判断：三轮共 3 条 BLOCKING，全部是主 Agent 维护这份冻结证据时的文字笔误，无一条指向产品缺陷。** 每修一次笔误 → 文件变 → 工作区身份变 → 需重新冻结 + 重新复审，形成自我延续的循环（复审对象包含复审自身的记录）。累计消耗 7 个身份、4 次复审派发。

   **处置（用户 2026-08-06 拍板，路线乙）**：不再阻塞第 12 条。理由：三轮复审已把产品侧查透 —— 编码变体覆盖、审批前真值解析为 0、TOCTOU 绑定、HTTP/env/stdin 边界、结果守卫 fail-closed、旧架构残留、假绿检测、xfail/skip 滥用、台账承重性均由独立执行体验过；继续派轮次是在验主 Agent 的文笔，不是在验产品。

   **诚实记录未取得的东西**：没有第三方以 PASS 形式为「这份冻结证据的文字表述完全自洽」签字。已知残余风险是**文档表述层**的，不是代码或制品层的。若后续要彻底闭环，治根办法是把叙述性文字从侧车剥离到独立的 `.r6-review-log.md`，侧车只留机器可校验键值（身份三项 + 读数 + `EXCLUDES` + `ALGORITHM`），使散文改动不再引入侧车不一致 —— 本轮未执行此改造。

   历史背景：R5 那次三路终审绑定 R5 候选身份（270 文件 / 27928 bytes / `c8a60656…`），R6 五刀后工作区已漂移，因此那次复审**覆盖不到 0.4.0 发布物**；`.r5-tdd-evidence.log` 的整个 R6 段（3300 行以后）grep 复审 / 终审 / reviewer / independent **零命中**。
2. **R6 冻结侧车已建** —— `.r6-freeze-evidence.sha256`，连续两次复算一致，侧车自身在 `EXCLUDES` 内故自排除自洽；当前身份见其 `R6_TECHNICAL_CANDIDATE_SHA256`，七个历史身份及各自读数见 `IDENTITY_HISTORY`（该文件散文一律不写死「当前身份」的哈希字面量，这正是第 2b 轮 B2 的成因）。各路复审均独立复算得同值且 `MATCHES_SIDECAR=True`，复审全程仓库零改动。**此项已关闭。**
3. **`KNOWN_GAP_2`**：`MIGRATION_V2_INVALID` 防御性兜底仍无测试覆盖（仅 `credential_guard/migration.py`）；兜底保留，**未解决**。
4. **provider live wire** strict xfail（`test_r5_provider_live_wire_decoy_count_zero`）仍在，**未解决**。
5. **AC8 / AC9** 本机日志 / state·sessions 残余明文仍为各自独立的 strict xfail，**未解决**（已接受的可信区后续项）。
6. 第 12 条：用户在正式 worker 上的安装验收尚未执行。
7. 敏感路径保护不覆盖任意动态拼接 / ctypes / 子进程绕过；不做无限递归 DLP。

### 剩余步骤与先后依赖

```text
① 台账修复回绿                     已完成（本报告「台账遗漏与修复」节）
② 本轮文档回填                     进行中：本报告 + HANDOVER.md + .r5-tdd-evidence.log
③ 建 .r6-freeze-evidence.sha256    锁定 0.4.0 身份；必须在 ② 全部落地后执行
④ 三路独立只读复审                 绑 ③ 的身份；此期间 repo 禁止任何写入
⑤ 用户亲自安装验收（第 12 条）      Agent 不代操作正式 worker
```

③ 必须在 ② 之后：侧车记录的就是「回填完成后的最终身份」，先冻结再回填会立刻作废侧车。

### 正式 worker 现状（与本报告同轮只读核实）

```text
worker 插件      0.2.0，plugins list 显示 not enabled，仅 mysql_credential_action
worker 配置店    已是单文件 credential-guard.json，但 http binding 缺 request 段
                 → 0.4.0 会以 CONFIG_SCHEMA 拒绝加载，安装前必须按指南 §5.2 补全
```

该结论经同形状合成配置实测复现（不读真实凭证值）：缺 `request` → `ConfigError code=CONFIG_SCHEMA`；补上 → `LOAD_OK`。指南 §5.2 三绑定模板（http / process_env / stdin）原样实测 `LOAD_OK`。注意 `inject.location` 必须是 `authorization_header`，写 `header` 会被拒。

---

## 历史 0.3.1（保留，非当前 designated）

M3.1 发布物（哈希原样，不作当前 designated）：

| artifact | path | sha256 |
|---|---|---|
| wheel | `dist/hermes_credential_guard-0.3.1-py3-none-any.whl` | `72b705f744b292ea2395c0e7ca70ef95b7ad25d9465fa0c42361f1bd511aeac3` |
| sdist | `dist/hermes_credential_guard-0.3.1.tar.gz` | `ea307f8f47a6328c8c8f3b8ff574d4d3c6c3cccf11b7328d7a54543759779bbb` |
| plugin zip | `dist/credential-guard-0.3.1-hermes-plugin.zip` | `7ebc8652d6a763a8ff9fa1d7596919e811bcff92b8eee572af30b61b54651ac6` |

历史 designated 报告仍为 `docs/M3-验收报告.md`。
