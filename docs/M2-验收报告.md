# M2 验收报告（2026-07-30）

## 结论

**PASS（L2 build 精确 key 集合收口；L1–L3 / L4 / J1–J7 / H1–H5、H7–H9 / K1–K3 保持；H6 经用户确认取消，非阻塞）**

按 `.m2-manifest-schema-final-fix.md`：`verify_artifact_manifest` 对 `build` 实施精确 key 集合（拒 `unexpected_build_key`），并对顶层/`build`/三 artifact entry 系统性 missing/extra/type/value mutation 红灯。连续两次独立干净构建三 artifact 字节完全相同。不得把取消 H6 写成技术失败。AC8/AC9 仍真实失败且非阻塞。

## 威胁边界（用户确认）

```text
可信：本机Hermes、Hermes标准审批机制、Credential Guard、本地凭证文件、本地执行器
不可信：外部模型、模型供应商、模型生成的参数、Provider返回内容
```

产品契约：`pre_tool_call` 要求审批；Hermes 批准才调用 handler，拒绝/超时则不调用。插件不防御 Hermes 宿主审批调度自身的缺陷，不实现第二套审批票据。

## 边界声明

| 项 | 证据 |
|---|---|
| Hermes 源码未改 | `git status --porcelain` 行数 = 0（`/Users/yelei/.hermes/hermes-agent`） |
| worker 未装插件 | `~/.hermes/profiles/worker/plugins/` 无 `credential-guard` |
| 现有 MySQL 未触碰 | Docker argv 白名单审计 + 前后指纹；报告只宣称「测试命令审计及前后指纹未发现触碰」 |
| 密码未进 argv/审批/tool result | 实测计数 0；argv 表述为「采样窗口未见」 |

## J1–J7 / H1–H9 / K1–K3 / L1–L4 收口状态

| 项 | 状态 |
|---|---|
| J1 真实发布产物 | **完成**：离线 setuptools 构建真实 wheel/sdist；最终安装物为 `credential-guard-0.2.0-hermes-plugin.zip`；解包后经真实 PluginManager 硬加载（enabled、无 load error、hooks/tool 身份、vendored `_ensure_pymysql`） |
| J2 release metadata 硬门禁 | **完成** |
| J3 candidate manifest | **完成**：生产源码 + 依赖 + 发布配置 + 构建器候选身份（白名单 `scripts/build_release_artifacts.py`，不含其余 scripts；不含 `artifact-manifest.json` 自身） |
| J4–J7 / H1–H5 / H7–H9 | **保持完成** |
| H6 approval ticket | **取消 / 超出插件目标 / 非阻塞** |
| K1 可复现发布构建 | **完成**：固定 epoch + 归档归一化；两独立 out 目录 build_all 三 hash 逐一相同 |
| K2 artifact-manifest 同源 | **完成**：严格等值门禁（version/`build` 精确 key/epoch/normalized_archives/pythonhashseed/tz/标准文件名/basename 路径/互异/sha256）；E2E `artifact_manifest_ok=true` |
| L1 构建器纳入 candidate | **完成**：仅篡改构建器 → 旧 manifest 验证失败；重建后 candidate 与 artifact hash 均变化 |
| L2 schema/路径 mutation 红灯 | **完成**：顶层/`build`/artifact entry 精确 key；缺字段/额外键（含 `build.unexpected_build_key`）/错误类型（bool 不冒充 int）/值漂移/`../`/绝对路径/重复名/坏 hash/deps 漂移 |
| L3 PluginManager 硬验证 | **完成**：禁止手工 `sys.path` 替代；负测删 `plugin.yaml`/破入口/删 deps 红灯 |
| L4 worker 分类规则 | **纠正**：`worker_live=true` + 完整 digest 无变化 → 可为 `unchanged`；仅 logs/WAL 噪声 → `inconclusive_live_noise`；不把 `worker_live=true` 一概写成不能 unchanged |
| K3 最新 worker 证据 | **完成**：designated run 见下 |

## 发布产物（本轮真实可复现构建）

| artifact | path | sha256 |
|---|---|---|
| wheel | `dist/hermes_credential_guard-0.2.0-py3-none-any.whl` | `4ed8e59ce6372bc1d698382bdcba45cfb9f092fdd95699d3a12ef008ceec24d7` |
| sdist | `dist/hermes_credential_guard-0.2.0.tar.gz` | `1c7805087b59fde1d9ae88d4ae5f0c538d5d802b4a88b1d0e49281fbb26e8617` |
| **最终安装物** | `dist/credential-guard-0.2.0-hermes-plugin.zip` | `fc031a4e52e65bec987b127c568649c1f699db09ab872fd54a3dbb11351bc566` |

构建配置身份：`SOURCE_DATE_EPOCH=1704067200`，`PYTHONHASHSEED=0`，`TZ=UTC`，归档元数据归一化。构建器：`/Users/yelei/.hermes/hermes-agent/venv/bin/python`（setuptools + vendored wheel，`--no-index` 离线）。同源记录：`dist/artifact-manifest.json`。

## 本轮 designated run（权威）

```text
.venv/bin/python -m pytest tests -q
→ 319 passed, 2 xfailed

.venv/bin/python -m compileall -q credential_guard tests scripts
→ COMPILE_OK

.venv/bin/python scripts/build_release_artifacts.py
→ RELEASE_BUILD_OK；连续两次独立构建三 artifact SHA 完全一致（见上表）

.venv/bin/python scripts/run_m2_e2e.py
→ success.gate=0 deny.gate=0
→ approval_choices success=["once","once"] deny=["deny"]
→ deny credentials_open/read/parse=0 secret_load=0 connect=0
→ provider_saw_blocked / fixed_blocked_result_ok / tool_result_reconcile_ok = true
→ blocked content = Hermes "BLOCKED: User denied..."（JSON error 包装；非脚本短语）
→ vendored_deps_manifest_sha256=efc9d3a469f8f29f457dc14ac1c51a8ef896a082f211555898c17440f3078738
→ candidate_manifest_sha256=e4d41cfcd33b359f934a736ae717ffea6f952f65f128391664d960094ca1742e
→ release_metadata_ok=true
→ artifact_manifest_ok=true
→ run_id=m2-1785385962-43505
→ worker_delta: worker_live=false, status=unchanged
→ config_plugins_sessions_unchanged=true

.venv/bin/python -m pytest tests/test_ac8_ac9_architecture_gate.py -q --runxfail
→ 2 failed（严格 xfail 在 --runxfail 下仍真实失败；非阻塞）

Docker：无 cg-m2-mysql-* 遗留；worker 未装插件；Hermes 源码 clean
```

审批证据说明：E2E 经测试 companion 安装的正式 Hermes `request_tool_approval` callback 自动作答；PTY 仅承载 CLI 与 argv 采样。不称「真实人工点击」。

## 历史（不可作为本轮放行依据）

- 此前因归档时间戳导致的 artifact SHA 漂移（含 plugin zip `eb1deea9…` / `5b812b7e…` / `3878576d…` / `1a7c1633…` 等）已作废。
- 旧 `run_id=m2-1785385027-36074`、`m2-1785383332-21104`、`m2-1785383504-22621`、`m2-1785383749-24671`、`m2-1785382762-17414` 及更早 run 仅历史证据。
- 本轮以 `run_id=m2-1785385962-43505` + `dist/artifact-manifest.json` 冻结 hash 为准。
