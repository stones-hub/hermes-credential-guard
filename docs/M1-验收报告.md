# M1 验收报告（2026-07-29，第六轮两项收口后）

## 验收命令与结果

- `.venv/bin/python -m pytest tests -q` → 退出码 `0`（`106 passed, 2 xfailed`）
  - `2 xfailed`：`tests/test_ac8_ac9_architecture_gate.py`（AC8、AC9 各自独立架构门禁，预期红）
  - 说明：项目 `.venv` 为 **Python 3.9.6**；真实 Hermes E2E 由 hermes **Python 3.11.15** 执行
- AC8：`.venv/bin/python -m pytest tests/test_ac8_ac9_architecture_gate.py::test_ac8_agent_log_must_be_zero_plaintext --runxfail -q` → **真实失败**（`agent.log` decoy count=1）
- AC9：`.venv/bin/python -m pytest tests/test_ac8_ac9_architecture_gate.py::test_ac9_state_and_sessions_must_be_zero_plaintext --runxfail -q` → **真实失败**（state/WAL/sessions total=2，主要为 WAL）
- `.venv/bin/python -m compileall credential_guard` → 退出码 `0`
- 本轮任务**未再运行** canary 覆盖主 Agent 快照（仅补测试与文档）

### 当前主 Agent 最新验收快照（真实 canary，不以本轮 Cursor 历史运行为准）

```text
worker_live=true
status=inconclusive_live_noise
stable=false
unchanged=false
config_plugins_sessions_unchanged=true
isolation.all_temp=true
```

结论：临时 `HERMES_HOME` 隔离已验证（`isolation.all_temp=true`）；worker 因活动日志/WAL 噪声无法判定完整不变，**不宣称** worker 未变化；`config` / `plugins` / `sessions` 未变。

### 历史运行快照（Cursor，仅供对照，非最新证据）

某次 Cursor 环境 canary 曾得到 `worker_live=false` / `status=unchanged` / `stable=true` / `unchanged=true` / `config_plugins_sessions_unchanged=true`。该结果仅反映当时 worker 空闲、完整 digest 未变；**不得**再当作最新验收结论。

链路：临时 `HOME`/`HERMES_HOME` → Python socket 常规入口 loopback guard → `hermes chat -q ... -Q -t context_engine --ignore-rules` → 插件 middleware → OpenAI SDK → `127.0.0.1` 假 provider。

## 第六轮修复项（F1—F2）

| 编号 | 问题 | 状态 |
|---|---|---|
| F1 | IPv6 正例矩阵缺 `connect_ex` / `create_connection` | **已修复**：`::1` listener + `skipif`；`connect_ex==0` 并 accept；`create_connection` 成功并 accept；审计含 `::1` |
| F2 | 报告把 Cursor `worker_live=false/unchanged` 当最新证据 | **已纠正**：主 Agent 最新快照为 `inconclusive_live_noise`；Cursor 结果标为历史运行 |

## 第五轮修复项（E1—E6）摘要

| 编号 | 状态 |
|---|---|
| E1 sendmsg `ancdata=None` | 已修复 |
| E2 网络守卫 spy + 正例矩阵 | 已修复；本轮 F1 补齐 IPv6 `connect_ex`/`create_connection` |
| E3 CLI check 直接 production callback | 已修复 |
| E4 canary 阻断提示门禁 | 已修复 |
| E5 worker 指纹诚实分类 | 已修复；本轮 F2 与主 Agent 最新快照对齐 |
| E6 AC8/AC9 独立 strict-xfail | 已拆分 |

## 正例矩阵证据（loopback）

| 入口 | IPv4 `127.0.0.1` | IPv6 `::1` | AF_UNIX |
|---|---|---|---|
| `connect` | 有 | 有 | 有 |
| `connect_ex` | 有 | **有（F1）** | — |
| `create_connection` | 有 | **有（F1）** | — |
| `sendto` | 有 | 有 | — |
| `sendmsg` | 有 | 有 | 有（socketpair） |

非 loopback 负例：spy 证明 `_ORIG_*` 调用次数为 0。Python socket 常规入口守卫 ≠ OS 网络沙箱。

## AC1—AC10 状态

- AC1：**通过** — 临时 HERMES_HOME + PluginManager `enabled=True`
- AC2：**通过** — middleware/hooks/cli 注册；`hermes credential-guard check` 只接受直接 production callback，退出码 0；伪造 `__wrapped__` 负测红；companion 存在时仍绿
- AC3：**通过** — `plain_secret_count=0`
- AC4：**通过** — opaque token + 注册表冲突矩阵
- AC5：**通过** — llm_execution 故障 HTTP=0、exit=0、固定阻断提示、stdout/stderr 无诱饵；llm_request 故障恰好 1 个安全 fallback
- AC6：**通过**（单测）
- AC7：**通过**（单测）
- AC8：**架构阻塞（预期红）** — 独立 `--runxfail` 失败；本轮实证 `logs/agent.log` decoy count=1
- AC9：**架构阻塞（预期红）** — 独立 `--runxfail` 失败；本轮实证 state/WAL/sessions total=2（wal=2）
- AC10：**通过** — 专项 106 passed；架构门禁 2 xfailed，不混称全绿

## AC8/AC9 seam 表述

> Hermes v0.19.0 没有覆盖 CLI、Gateway、ACP、TUI、cron、subagent 等所有 agent 入口、受支持且 fail-closed 的统一 pre-log/pre-persist seam。

例外：

- Gateway 普通外部消息已有 `pre_gateway_dispatch` rewrite，可在 gateway 子集早期改写；
- 但不覆盖 CLI/ACP/TUI/cron/subagent/internal event，异常仍 fail-open，不能作为全局安全边界。

### AC9 口径（待用户拍板）

本项目最终安全目标是「本地持久化零明文」还是仅「披露本地残余风险」，需用户拍板。在拍板前，architecture gate 继续按**零明文**保持红灯（AC8/AC9 各自 `--runxfail` 实证仍红）。

### 后续方案

- **A.** 接受本机可信边界：插件目标仅保证外部不泄漏（AC1—AC7）。
- **B.** 对 Hermes 做最小核心补丁 / 官方统一 fail-closed pre-persist hook，覆盖全部 agent 入口。

## 状态三分类

### 插件代码通过

- E1—E6 + F1 IPv6 `connect_ex`/`create_connection` 正例
- E2 Python socket 常规入口完整矩阵（spy 证明非 loopback 零真发包）
- E3 CLI check 仅绑定直接 production callback identity
- E4 canary 完整门禁（阻断提示 + 无诱饵）
- E5 worker 指纹诚实分类 + 临时隔离硬断言
- 外发原值 0、opaque 引用、无 tools、exit 0

### 测试隔离证据通过/不确定

- **通过**：子进程 `HOME` / `HERMES_HOME` / cwd / config / state / logs 均为临时目录（`isolation.all_temp=true`）
- **当前主 Agent 最新快照**：`worker_delta.status=inconclusive_live_noise`（`worker_live=true`，`stable=false`，`unchanged=false`）；**不宣称** worker 未变；`config_plugins_sessions_unchanged=true`
- **历史 Cursor 运行**：曾出现 `worker_live=false` / `status=unchanged`，仅作对照
- config/plugins/sessions 变化仍硬失败

### Hermes 架构边界仍阻塞

- AC8：`agent.log` decoy count=1（本轮实证）
- AC9：state/WAL/sessions total=2（本轮实证，主要为 WAL）
- 宿主 PluginManager / middleware / execution chain 自身异常默认 fail-open
- Python socket guard ≠ OS 网络沙箱

## 是否建议进入 M2

**不建议进入 M2。** 未安装到 worker。AC8/AC9 方案未拍板前不进入文件后端。
