# R10 / 0.4.4 验收报告（技术候选 PASS · 待发布）

**状态：技术候选 PASS（含 artifact entry `size` 严格绑定、最终 ZIP 验收及两路独立只读终审）。** 0.4.4 四制品已由双临时构建 `cmp` 一致后 `shutil.copy2` 写入 `dist/`；**尚未 commit / push / Tag / GitHub Release / 尚未安装正式 worker。** 基线提交 `ed91c98057bf010ff5a3d3b84ee8bb97c7c5ed9d`（v0.4.3）。

## 产品目标

Credential Guard 0.4.4 窄修：协议骨架字段 value 命中已登记凭证变体时本机 fail-closed（`LocalBlockRequest`，Provider=0）。普通 content / arguments 仍安全替换后继续。方案：`docs/R10-0.4.4-协议字段已登记凭证失败关闭修复方案.md`。

发布身份合同补齐：artifact-manifest 三个制品 entry 必须绑定 `filename` / `sha256` / `size`，verifier 与磁盘 `stat().st_size` 精确一致（本轮阻断修复）。

## 候选身份

| 项 | 值 |
| --- | --- |
| 基线 commit | `ed91c98057bf010ff5a3d3b84ee8bb97c7c5ed9d` |
| `plugin.yaml` version | **0.4.4** |
| `candidate_manifest_sha256` | `dae1ebdb8b992f4381646a7d59d116473cc9e8b8fddc54a77757566fd1f016e8` |
| `SOURCE_DATE_EPOCH` | `1704067200` |
| `PYTHONHASHSEED` | `0` |
| `TZ` | `UTC` |
| 构建 Python | `/Users/yelei/.hermes/hermes-agent/venv/bin/python` |
| 0.4.4 四制品 | **已落地（`ARTIFACTS_LANDED=True` / `STRICT=True`；含 size 绑定）** |

## 0.4.4 发布物（designated · size 绑定后双临时构建 copy2 实测）

| 种类 | 文件名 | SHA-256 | size |
| --- | --- | --- | --- |
| plugin zip | `credential-guard-0.4.4-hermes-plugin.zip` | `d6ee2bf6a92a4ca55ee37f24802cf26316ab38adcbe27b9d59a4ee9e944ae265` | `112548` |
| wheel | `hermes_credential_guard-0.4.4-py3-none-any.whl` | `74371c32da35913f3c371db3b98871dad3dd192c0172bb37bea38546a3c0462e` | `114120` |
| sdist | `hermes_credential_guard-0.4.4.tar.gz` | `96c15aa8129446f55f4ad1ee9df36ac3d9479726fbfb47bf39cdee4288b2ecb4` | `96861` |
| versioned manifest | `artifact-manifest-0.4.4.json` | `a3508d2de9521a3b4381d28425960737f2c9876ea84f7831fc29f019d9137b9a` | `871` |

大小（字节）：ZIP `112548`；wheel `114120`；sdist `96861`；manifest `871`。manifest 三 entry 的整数 `size` 分别等于对应制品磁盘大小；manifest 自身不加 `size` 字段。

## 双构建与历史零漂移

- 两次独立临时目录 A=`/tmp/cg044-size-fix-a`、B=`/tmp/cg044-size-fix-b` 调用生产 `build_all(tmp)`（禁止真实 `dist/` 作 out_dir；未调用 `main()`）。
- 四文件 `cmp -s` 全部 exit 0；SHA-256 / size 与上表一致后，本阶段仅用 Python `shutil.copy2` 从 A 落地到仓库 `dist/`。
- 阶段保持：`CURRENT_DIST_PHASE=artifacts_landed`；`ARTIFACTS_LANDED_FOR_CURRENT=True`；final-ZIP harness `ARTIFACTS_LANDED=True` + 真实 plugin ZIP SHA 回填；`STRICT=True` 保留。
- 历史 0.4.2 / 0.4.3 八文件相对本轮开工前 **零漂移**（未删除、未覆盖）。

## 历史 0.4.3 四制品锚点（必须零漂移）

| 种类 | 文件名 | SHA-256 |
| --- | --- | --- |
| plugin zip | `credential-guard-0.4.3-hermes-plugin.zip` | `738bc8ae4e1973a50efba604602a9fb3c7a6739efb95e48024b6a1975e97dacb` |
| wheel | `hermes_credential_guard-0.4.3-py3-none-any.whl` | `a7bf91e1ceb263fc2c1f468baac7c6990d6c2899c27409704e31fff13bb1752c` |
| sdist | `hermes_credential_guard-0.4.3.tar.gz` | `5f7b939ca37ff6ef38eaaaba381536cafdc0196a80b558705e7016997282033b` |
| versioned manifest | `artifact-manifest-0.4.3.json` | `86321bd8fc7ee4be994d3202f368bae114a23fd18bdea257eb5e810c96231127` |

## 协议字段已登记凭证窄修（严格 TDD）

| 阶段 | 结果 |
| --- | --- |
| RED（生产未收窄） | `tests/test_protocol_field_registered_secret.py`：**6 failed / 5 passed**；四字段均把 synthetic 换成 `<SECRET:…>` 后继续（非语法/fixture 错误）；mutation 因判定函数尚不存在 AttributeError；whole-field allowlist 复用否定例同 RED |
| GREEN | 同文件 + `test_protocol_field_whole_field_fallback.py`：**18 passed** |
| mutation | 绕过 `_is_protocol_field_value_path` → 旧 Provider=1 重开；健康代码下 mutation 测试本身 GREEN |

## 强制验证读数（size 绑定收口后实测）

| 项 | 结果 |
| --- | --- |
| size schema/mutation 专项 | RED **49 failed / 1 passed**（机制=`unexpected keys: ['size']`）；GREEN **49 passed** |
| `tests/test_reproducible_release.py` 全文件 | **108 passed** |
| 协议字段已登记凭证专项 + whole-field | **18 passed**（无回归） |
| dist policy / reproducible / composition / opt-in / README / topology | **270 passed**（含于组合跑次） |
| 0.4.4 final-ZIP E2E `scripts/run_r10_044_final_zip_tests.py` | **4 passed**（真实临时安装 + PluginManager + source-fallback mutation） |
| `scripts/run_r5_nobuild_pytest.py` | **1960 passed, 3 xfailed, 0 failed**（exit 0） |
| compileall `credential_guard tests scripts` | **exit 0** |
| `git diff --check` | **exit 0** |
| 0.4.2 / 0.4.3 八制品 SHA-256 复算 | **零漂移（开工/收工一致）** |
| 0.4.4 `dist/` 精确 12 文件；三 entry size = 磁盘 | **与上表一致；verifier PASS** |

## 两路独立只读终审

- 第一路：协议字段 18 passed、manifest/release 108 passed、final ZIP 4 passed、制品与文档只读核验通过；临时复审 brief 移出候选后，原受影响发布组合 297 passed，关闭确认结论 **PASS**。
- 第二路：独立重跑协议字段 18 passed、manifest/release 108 passed、final ZIP 4 passed、发布组合 297 passed；manifest verifier、dist 12 文件、历史 0.4.2/0.4.3 零漂移及开闭场状态全部通过，结论 **PASS**。
- 两路均未写仓库、未 build/install、未联网、未访问真实 Profile/凭证；`git diff --check` exit 0。

## 版本字面量分类（摘要）

- **当前产品版本**：0.4.4（`plugin.yaml` / `pyproject.toml` / `release_identity` / builder / README 安装配方）
- **当前可核验安装**：`credential-guard-0.4.4-hermes-plugin.zip`（上表 SHA）
- **历史证据**：0.3.1–0.4.3 报告 / runner / `dist/` 四制品保留，不改写、不覆盖

## 当前 0.4.3 发布真相（状态区）

0.4.3 已完成 commit / push / tag / GitHub Release（见仓库 `v0.4.3`）。本报告不改写 `docs/R9-0.4.3-验收报告.md` 当时「尚未 Tag」的历史表述。

## 未做

- Tag / GitHub Release（0.4.4）
- 正式 worker 安装 / 启用 / 重启
- 向真实 `dist/` 重跑 `build_all` / `main()`（本轮仅 `copy2`）
- commit / push
