# M3 / M3.1 验收报告（0.3.1 当前 designated）

## 结论

**PASS（0.3.1）**

M3.1 C 轮（编码变体、门禁一致性、0.3.1 发布收口）按严格 TDD 完成。全量 pytest 0 failed（2 xfailed=AC8/AC9）；compileall exit 0；canary / M2 / M3 Framework E2E 均 exit 0；最终 plugin ZIP 隔离安装后的编码 canary wire proof exit 0；双构建三制品字节一致。worker / 真实 `~/.ssh` / Hermes 源码未改动。

历史 M3 0.3.0 发布物与结论保留于下文「历史 0.3.0」段，**不再**作为当前 designated hash。

## 威胁边界

```text
可信：本机Hermes、Hermes标准审批机制、Credential Guard、本地凭证文件、本机OpenSSH与SSH文件、本地执行器
不可信：外部模型、模型供应商、模型生成的参数、Provider返回内容
```

产品契约：

```text
审批前：可信外发守卫可读 credentials.json，仅建立本次外发内存脱敏快照；执行器不解析本次 target 凭据。
审批拒绝/超时：handler 不调用；execution secret resolution / MySQL connect / SSH subprocess = 0。
审批通过：handler 按 credential_ref 解析本次凭据；目标系统权限仍由 MySQL/SSH 决定。
```

## M3.1 C 轮 TDD RED→GREEN（摘要）

| 切片 | RED | GREEN |
|---|---|---|
| T1 percent/quote-plus | 变体进入 next_call | 同 token 替换；工具结果同步 |
| T2 Base64/URL-safe | 变体进入 next_call | 替换；`contains_plain_secret` 覆盖变体 |
| T3 JSON escape | `\u` / `\"` 漏过 | 替换；原对象不变 |
| T4 Basic Auth | `base64(user:pass)` 进 Provider | snapshot 注册 `basic_auth`；check 只报 opaque |
| T5 编码 PEM | 四类编码进 Provider/工具回流 | 有界扫描阻断；正例不误拦 |
| T6 资源上限/故障 | （界限常量 + fail-closed 断言） | 下游 0；安全 JSON；日志不回显 |
| T7 gate mutation | success 未门禁 host/db/ref | 全安全字段 mutation；wire counts；accepted_local_residual 不入 gate |
| T8 跨版本 hash | M2 0.2.0 hash 误比当前 manifest | 按 PLUGIN_VERSION 选 designated 报告；M2 历史 hash 冻结 |
| T9 版本 0.3.1 | — | plugin.yaml / pyproject / release_identity / builder |
| T10 双构建+ZIP | — | 见下文真实证据 |

## 当前验收命令结果（0.3.1）

| 命令 | 结果 |
|---|---|
| `.venv/bin/python -m pytest tests -q -p no:cacheprovider` | **462 passed, 2 xfailed** |
| `python3 -m compileall -q credential_guard scripts tests` | **exit 0** |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/run_canary_e2e.py` | **exit 0**；wire plain/percent/quote-plus/base64/urlsafe/basic-auth **全部 0**；`stable_ref_count=7`；`accepted_local_residual` 仅诊断 |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/run_m2_e2e.py` | **exit 0**；`success.gate=0`（`run_id=m2-1785470193-44009`）；`deny.gate=0`；deny 路径 `credential_secret_load_count=0`，`accepted_egress_store_reads` 为外发快照诊断 |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/run_m3_e2e.py` | **exit 0**；success/deny/path_block/negatives **四 gate=0**（`run_id=m3-1785470153-43503`） |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/run_final_zip_encoding_canary.py` | **exit 0**；`installed_from_zip=true` 且运行时模块位于隔离插件目录、非源码树；编码 wire counts 全部 0；真实含 `-`/`_` 的 URL-safe PEM helper 命中、Provider 调用 0、工具结果阻断；`stable_ref_count=7` |
| 双独立 out 目录 `build_all` | 三 artifact **字节完全一致** |

说明：M2 deny 不再把 `credentials_open/read/parse` 当作拒绝门禁（M3.1-A：审批前外发守卫可读秘密库）；执行门禁仍为 `credential_secret_load_count=0` 与 `mysql_connect_count=0`。

## 0.3.1 发布物（当前 designated）

| artifact | path | sha256 |
|---|---|---|
| wheel | `dist/hermes_credential_guard-0.3.1-py3-none-any.whl` | `72b705f744b292ea2395c0e7ca70ef95b7ad25d9465fa0c42361f1bd511aeac3` |
| sdist | `dist/hermes_credential_guard-0.3.1.tar.gz` | `ea307f8f47a6328c8c8f3b8ff574d4d3c6c3cccf11b7328d7a54543759779bbb` |
| plugin zip | `dist/credential-guard-0.3.1-hermes-plugin.zip` | `7ebc8652d6a763a8ff9fa1d7596919e811bcff92b8eee572af30b61b54651ac6` |

- `SOURCE_DATE_EPOCH=1704067200`，`PYTHONHASHSEED=0`，`TZ=UTC`，归档归一化
- `candidate_manifest_sha256=044fecb1fdad725da43c2e644f6d0bf0c4263fe89aac05c9451f69e4f456fa72`
- `vendored_deps_manifest_sha256=efc9d3a469f8f29f457dc14ac1c51a8ef896a082f211555898c17440f3078738`（与 M2 相同）
- 两个独立干净 out 目录构建：三 artifact SHA **完全一致**
- 最终 ZIP：禁止内容扫描 + 真实 PluginManager 硬加载 + 隔离 Hermes CLI 编码 canary wire proof

## worker / Hermes 未变化证据

- canary/M2/M3：`worker_delta` 为 `unchanged` 或可接受的 `inconclusive_live_noise`（config/plugins/sessions 未改）
- Hermes 源码：`git -C ~/.hermes/hermes-agent status --porcelain` 行数 = 0
- worker 仍保留既有 **0.2.0** 插件目录；本轮**未**安装/升级为 0.3.1

## 尚存边界

1. 敏感路径保护覆盖标准文件/搜索工具与可识别的直接 terminal 读取；不覆盖任意动态拼接、ctypes、子进程绕过。
2. 编码变体保护为有界确定性表示 + 有界 PEM 解码扫描；不做无限递归 DLP。
3. AC8/AC9 本机日志/WAL 残余明文仍 strict-xfail，非阻塞（字段名 `accepted_local_residual`）。
4. worker 升级到 0.3.1 需用户另行确认；本轮隔离验收不触碰。
5. 未读取真实 `~/.ssh`；未连接真实 SSH/DB；未改系统 sshd 配置。

---

## 历史 0.3.0（保留，非当前 designated）

M3 功能主体于 2026-07-30 完成时的发布物（仅历史证据）：

| artifact | path | sha256 |
|---|---|---|
| wheel | `dist/hermes_credential_guard-0.3.0-py3-none-any.whl` | `2c9d6d14df2e61cbea42e066b44352afc8f7ef0de7c55370798235c8ef05f55a` |
| sdist | `dist/hermes_credential_guard-0.3.0.tar.gz` | `340379ced61cb9c342b0bbdda4bdfd4465d51badc3146e6b3537fdcf647b0e03` |
| plugin zip | `dist/credential-guard-0.3.0-hermes-plugin.zip` | `edc6bc67f1fb0aff966256034e7dedf9b8057aa56df7815e9ee7f0e1714ba83a` |

- 当时 `candidate_manifest_sha256=099cecef866d04aab4361cd2b217df6bc63a26b0dfddb27a195cf5bcc794b64a`
- 历史全量口径：391 passed, 2 xfailed（其后 M3.1 复审另计）
