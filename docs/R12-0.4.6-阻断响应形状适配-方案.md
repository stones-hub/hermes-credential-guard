# R12 / 0.4.6 方案：本机阻断响应形状未适配 api_mode

- 状态：**待审批**（只出方案，未改任何代码）
- 提出日期：2026-08-24
- 影响版本：0.4.5（含）及之前所有版本
- 目标版本：0.4.6
- 取证脚本：`/Users/yelei/tmp/cg-test/plan_evidence.py`（只读，直接调用宿主 transport 与插件函数）

---

## 1. 现象

`api_mode: anthropic_messages` 的用户触发任何一次本机阻断（fail-closed）时：

| 位置 | 用户实际看到的 |
|---|---|
| CLI 主输出 | `Invalid API response after 3 retries: response time 21.0s`，退出码 1 |
| 等待时间 | 约 20 秒（3 次重试 + 退避） |
| stderr | 仅 `DIAGNOSTIC_REASONS` 那段中文诊断（配置类失败才有） |
| **丢失的内容** | **`format_block_message()` 生成的六行阻断消息，含 `代码：CG-XXX`** |

对照组：填一个不存在的模型名 → `HTTP 503 无可用渠道`，原因清晰。**说明这不是宿主对所有失败的通用表现。**

### 1.1 危害定性

- **不泄密**。阻断本身成功了：Provider 调用次数 = 0，凭证没有外发。
- **属可用性 / 可诊断性缺陷**：把「本机安全阻断」伪装成「上游 API 故障」，把排查方向从"检查本机配置"引向"检查中转站/网络/限流"。
- 真实场景代价：`chmod 600` 就能解决的问题，用户可能先去排查半小时上游。

---

## 2. 根因（已实测取证，非推断）

### 2.1 链路

```
credential_guard/middleware.py:684  _safe_blocked_response()
    └─ 返回 SimpleNamespace(id, object, created, model, choices=[...], usage)
                                                     ▲ 只有 OpenAI 形状

agent/transports/anthropic.py:194   AnthropicTransport.validate_response()
    └─ content_blocks = getattr(response, "content", None)
       if not isinstance(content_blocks, list): return False
                                                     ▲ 要 Anthropic 形状

agent/conversation_loop.py:3288-3295
    elif agent.api_mode == "anthropic_messages":
        if not _tv.validate_response(response):
            response_invalid = True
            error_details.append("response.content invalid (not a non-empty list)")

agent/conversation_loop.py:3317-3343
    if response_invalid: → _invoke_api_request_error_hook(error_type="InvalidAPIResponse",
                                                          retryable=True)
                          → retry_count += 1 → 重试直至耗尽 → "Invalid API response after N retries"
```

### 2.2 取证结果（`plan_evidence.py` 实跑输出）

| 编号 | 验证项 | 结果 |
|---|---|---|
| F1 | 插件当前阻断响应的字段 | `['choices','created','id','model','object','usage']`，**无 `.content`** |
| F1 | `AnthropicTransport.validate_response(当前对象)` | **`False`** ← 根因坐实 |
| F2 | `validate_response(Anthropic 形状对象)` | **`True`** ← 修法方向可行 |
| F3 | `normalize_response(Anthropic 形状)` 能否还原正文 | **正文完整还原 `True`，含 `CG-CONFIG-UNAVAILABLE` `True`**，`finish_reason='stop'`，`tool_calls=None` |
| F4 | `api_mode` 是否在 `llm_execution` kwargs 中 | **在**。`conversation_loop.py:3187` 明写 `api_mode=agent.api_mode`，且 `run_llm_execution_middleware` 透传 `**context` |
| F5 | 当前形状对其他 api_mode 的兼容性 | `chat_completions.validate` = **`True`**；`bedrock.validate` = **`True`** |

**F5 的意义**：当前实现对 OpenAI 系与 Bedrock 是正确的，**只有 `anthropic_messages` 这一条路是坏的**。修复必须是增量适配，不能改动已经正确的分支。

---

## 3. 与「README 诊断说明不准确」的关系（原清单第 2 项）

原判断：「README:893 说去找 `CG-CONFIG-UNAVAILABLE`，实测五种故障场景一次都没出现」。

**该判断不准确，本方案予以更正**：

- 该阻断码**确实存在**（`middleware.py:174`），由 `format_block_message()` 写入阻断响应体的 `代码：` 行。
- 之所以"从未出现"，正是因为**这条消息装在被本 bug 吞掉的那个响应体里**。
- stderr 里能看到的那段中文（`配置加载失败 / 文件 / 位置 / 原因 / 处理`）来自另一条独立通路 `_text_for_diagnostic_event()`（`middleware.py:391`），它不含 `代码：` 行。

**结论：清单第 1 项与第 2 项是同一个 bug 的两个症状。修复本项后，阻断码会正常出现在主输出，README 的原始表述即自动成立。**

README 已先行更新为准确描述（含「已知问题」提示块），本项修复合入后需回改该提示块。

---

## 4. 修法选型

### 方案 A：按 `api_mode` 分支构造（推荐）

```python
def _safe_blocked_response(detail=None, api_mode=None):
    ...
    if api_mode == "anthropic_messages":
        return _blocked_response_anthropic(text)
    return _blocked_response_openai(text)   # 现状，覆盖 chat_completions / bedrock
```

`on_llm_execution` 从 kwargs 取 `api_mode` 传下去。

| | |
|---|---|
| 优点 | 精确；不动已正确的 OpenAI/Bedrock 分支；下游 `normalize_response` 契约已由 F3 验证可用 |
| 缺点 | 未来新增 api_mode 需同步适配 |
| 风险 | 低。`api_mode` 拿不到时退化为现状（等价于当前行为，不会更糟） |

### 方案 B：单个对象同时携带 `choices` + `content`

| | |
|---|---|
| 优点 | 一劳永逸，不依赖 `api_mode` |
| 缺点 | 响应对象语义混乱；`normalize_response` 会同时看到两套数据，行为未验证；对未来 transport 的副作用不可预期 |
| 风险 | **中高**。属于"骗过校验"而非"符合契约" |

### 方案 C：A + B 兜底

`api_mode` 已知走 A；未知走 B。

| | |
|---|---|
| 优点 | 最稳 |
| 缺点 | 引入 B 的全部未验证风险，换取一个 F4 已证明不会发生的边界情况 |

### 推荐：**方案 A**

理由：F4 已证明 `api_mode` 稳定可得；F3 已证明 Anthropic 形状被下游正确消费；F5 已证明现有分支无需改动。B/C 的额外复杂度没有对应收益。

---

## 5. 影响面

| 项 | 说明 |
|---|---|
| 改动文件 | `credential_guard/middleware.py`（`_safe_blocked_response` + `on_llm_execution` 取参） |
| 调用点 | `middleware.py:1633 / 1638 / 1641 / 1644` 共 4 处 |
| 行为变化 | 仅 `anthropic_messages` 用户：阻断从"重试 3 次报 API 故障"变为"立即显示中文阻断消息" |
| 不变 | OpenAI / Bedrock 用户行为**零变化**（F5 已证明当前形状对它们有效） |
| 安全语义 | **零变化**。阻断依然是阻断，Provider 调用次数依然为 0，不放宽任何检查 |
| 配置兼容 | 无配置变更，无迁移 |

---

## 6. 验证方案（严格 RED → GREEN）

### 6.1 先 RED

新增测试，在**未修复**代码上必须失败：

1. `test_blocked_response_passes_anthropic_validate` —
   构造 `_config_unavailable_detail()`，取 `_safe_blocked_response(detail, api_mode="anthropic_messages")`，
   断言 `AnthropicTransport.validate_response(...) is True`。**未修复时为 RED。**
2. `test_blocked_message_survives_anthropic_normalize` —
   断言 `normalize_response(...).content` 完整包含 `format_block_message(detail)` 全文及 `代码：CG-CONFIG-UNAVAILABLE`。
3. `test_openai_shape_unchanged` —
   `api_mode=None` / `"chat_completions"` / `"bedrock_converse"` 时形状与现状逐字段一致（**回归护栏，未修复时应为 GREEN，修复后仍须 GREEN**）。

### 6.2 mutation 承重

- 偷偷把 Anthropic 分支改回 OpenAI 形状 → 测试 1、2 必须转 RED
- 偷偷把 OpenAI 分支改成 Anthropic 形状 → 测试 3 必须转 RED

（防止"两个错误互相抵消"式假绿）

### 6.3 端到端

在隔离 `HERMES_HOME` + 真实 `anthropic_messages` provider 下，人为制造 fail-closed（`chmod 644` 配置文件），断言：

- CLI 主输出出现中文阻断消息与 `代码：`
- **不**出现 `Invalid API response`
- 退出耗时 < 5 秒（不再空等 3 次重试）
- Provider 实际收到请求数 = 0

### 6.4 全量

`pytest`（非构建）全量必须与基线一致，0 failed。

---

## 7. 覆盖范围决策

| api_mode | 当前是否可用 | 本次是否处理 |
|---|---|---|
| `chat_completions`（OpenAI 系） | ✅ F5 = True | 不动，加回归护栏 |
| `bedrock_converse` | ✅ F5 = True | 不动，加回归护栏 |
| `anthropic_messages` | ❌ F5 = False | **本次修复** |
| `codex_responses` | ❓ 取证时 transport `ImportError`，未验证 | **待定 —— 见下** |

**`codex_responses` 处理建议**：本次先补一个 `xfail` 标记的探测测试记录未知状态，不盲目适配。理由：无法在本机加载该 transport，未经实测的适配等于凭空猜测，违反"未验假设不写进方案"的原则。若用户有 codex provider 可测，则纳入本次范围。

---

## 8. 发布计划（0.4.6）

前置：本方案经用户审批。

1. 严格 RED → GREEN 落地 §6.1
2. mutation 承重 §6.2
3. E2E §6.3
4. 全量测试 §6.4，连续两次稳定
5. 回改 README §3 的「已知问题」提示块
6. 版本号升 0.4.6（`pyproject.toml` + `plugin.yaml`）
7. 双次构建四制品（ZIP / wheel / sdist / 清单），核对 SHA-256 可复现
8. 从新 ZIP 做隔离安装验收
9. **独立只读复审全 PASS**
10. GitHub Release
11. 用户确认后升级 default 并实测

**不做**：不删除或重打 v0.4.5 tag。0.4.5 制品保持原样，另发 0.4.6。

---

## 9. 待用户决策项

1. 是否采纳**方案 A**（vs B / C）
2. `codex_responses` 是否纳入本次范围（取决于是否有可测的 codex provider）
3. 是否授权进入编码阶段
