# 全量测试失败的真实定性（实测，2026-08-20）

**基线提交**：`2d4c25d`（plugin.yaml `version: 0.4.4`，已发布上线的版本）

> **重要**：本文件替代早先那份「17 条已知基线，全都别碰」的判断。那份判断是错的，会误导执行者把本来能绿的测试当成豁免。

---

## 0. 测试执行器（必读）

```
/Users/yelei/data/code/repos/hermes-credential-guard/.venv/bin/python -m pytest
```

系统 `python3` 没有 pytest，`which python` / `which pytest` 都找不到。

**必须设 `PYTHONPATH`**，否则 `from tests.fake_provider import FakeProvider` 会 ImportError：

```python
env = dict(os.environ)
env["PYTHONPATH"] = "/Users/yelei/data/code/repos/hermes-credential-guard"
subprocess.run([PY, "-m", "pytest", ...], cwd=REPO, env=env)
```

---

## 1. 结论速览

全量跑测得 17 failed，逐条单跑复验后重新定性：

| 组 | 条数 | 定性 | 处理 |
|---|---|---|---|
| A | 6 | **假失败** —— 全量跑时的测试间污染 | **必须绿**，不得豁免 |
| B | 9 | **测试自身缺陷** —— `sys.setprofile` 观测不可靠 | 豁免，另案处理 |
| C | 1 | **测试自身缺陷** —— 同上 | 豁免，另案处理 |
| D | 2 | **新增文件未登记** | 登记路径解决 |

**产品缺陷：0 条。** 本轮排查未发现任何真实的生产代码缺陷。

---

## 2. A 组 · 假失败（6 条，必须绿）

```
tests/test_hermes_cli_e2e.py::test_hermes_chat_real_cli_e2e_redacts_outbound_http
tests/test_hermes_cli_e2e.py::test_hermes_chat_long_system_prompt_does_not_false_block
tests/test_hermes_cli_e2e.py::test_hermes_chat_fail_closed_llm_execution_http_zero_exit_zero
tests/test_hermes_cli_e2e.py::test_hermes_chat_fail_closed_llm_request_provider_zero
tests/test_hermes_cli_e2e.py::test_hermes_chat_plugin_disabled_no_interference
tests/test_result_guard_authenticity_gate.py  （该文件其余 4 条）
```

**实测证据**：

```
单独跑 tests/test_hermes_cli_e2e.py       ->  rc=0   6 passed
```

全量跑时报 `non-loopback connect attempted: api.github.com:443`，是**别的测试**触发的对外连接被记进了共享的网络审计文件，导致这一批一起被判失败。

**早先误判**：我曾据此认定「Hermes 本体连了 GitHub，属环境问题」。这是错的 —— 没做最基本的单跑对照。

**要求**：这 6 条单独跑必须全绿。**不得列入豁免清单。**

---

## 3. B 组 · 测试自身缺陷（9 条，豁免）

```
tests/test_r3c_wire_e2e.py::test_r3c_wire_http_approve
tests/test_r3c_wire_e2e.py::test_r3c_wire_env_approve
tests/test_r3c_wire_e2e.py::test_r3c_wire_stdin_approve
tests/test_r3c_wire_e2e.py::test_r3c_wire_http_timeout_distinct_from_deny
tests/test_r3c_wire_e2e.py::test_r3c_wire_env_timeout_distinct_from_deny
tests/test_r3c_wire_e2e.py::test_r3c_wire_stdin_timeout_distinct_from_deny
tests/test_r3c_wire_e2e.py::test_r3c_wire_http_replay_closed
tests/test_r3c_wire_e2e.py::test_r3c_wire_env_replay_closed
tests/test_r3c_wire_e2e.py::test_r3c_wire_stdin_replay_closed
```

单独跑该文件：`9 failed, 23 passed`（同文件 23 条过）。

### 失败的是什么

唯一挂掉的断言是 `tests/test_r3c_wire_e2e.py:98`：

```python
assert r["order"] == _FULL_APPROVE_ORDER
# 期望 ['tool_request','tool_execution','pre_tool_call','approval_gate',
#       'handler','consume','resolve','adapter']
# 实际 ['approval_gate']
```

`order` 由 `scripts/run_r3c_wire_e2e.py:993` 的 `sys.setprofile` 钩子采集。

### 为什么判定是测试缺陷而非产品缺陷

**一、同一结果对象里，所有功能断言都成立**

```
approval_outcome      = 'approved'    审批确实批准
approval_raw_count    = 1             审批框确实弹出
wire_secret_count     = 0             真值零泄漏
token_in_provider_raw = 0             模型未拿到真值
```

功能是好的，挂掉的只有「通过函数调用监视观察到了几步」。

**二、`sys.setprofile` 本身就是不可靠的观测手段**

一个进程只能有一个 profile 钩子（后设者覆盖先设者）；pytest、coverage、debugger 都会争抢；函数内联、C 层调用、跨线程都会导致静默漏采。

**三、脚本作者自己就知道它不稳**

`scripts/run_r3c_wire_e2e.py:1291`：

```python
# Deduplicate order to first full seam sequence (AIAgent may re-enter frames).
```

对不稳定的观测结果做去重补丁，本身就是承认该断言不可靠。

**四、这个版本已经发布上线**

`2d4c25d` 是已发布版本。若审批链路真的只走 1 步，注入功能根本不可用，不可能发布。独立黑盒实测也确认：审批框正常弹出、注入正常执行、真值零泄漏。

### 处理

**豁免**，但**不是「跑不过就算了」** —— 是「有证据表明测试的观测手段有缺陷」。

建议另案改造：用生产代码内的显式可注入检查点替代 `sys.setprofile`，或直接断言副作用而非调用顺序。**不在 C1/C2 范围内。**

---

## 4. C 组 · 测试自身缺陷（1 条，豁免）

```
tests/test_result_guard_authenticity_gate.py::test_live_pluginmanager_transform_enters_result_guard
```

同一病根。`tests/test_result_guard_authenticity_gate.py:374` 同样用 `sys.setprofile`。

**自证据**（同一次运行、同一个钩子）：

```
run_conversation_entered = True     ← 抓到了
guard_entered            = False    ← 没抓到
```

一个抓到一个没抓到，说明是钩子漏采，不是 guard 未执行。测试挂在第 517 行的观测断言上，**真正的功能断言（`provider_secret_count == 0`、`auth_residue == 0`、`pem_residue == 0`）根本没机会执行**。

---

## 5. D 组 · 新增文件未登记（2 条，须解决）

```
tests/test_r5_topology_gate.py::test_live_workspace_uses_phase_detector_and_classify
tests/test_r3c_historical_identity_gate.py::test_r3c_reclosure_identity_is_layered_from_r4_workspace
```

带本任务新增文件时报：

```
unclassified live paths: ['.c1c2-usability-task.md', 'docs/R11-0.4.5-...方案.md']
final: preserved drifted: docs/R3-通用本地凭证注入-落地方案.md
```

这是仓库的工作树身份门禁，新增文件本就应当登记。

**处理**：把本任务新增的文件路径登记进 `R5_ADDED_PATHS` / `R5_MODIFIED_PATHS` / `_R5_PLANNING_DELTA_PATHS`。**只登记路径，不得改动这两个文件中的任何断言逻辑。**

---

## 6. C1/C2 验收标准

### 必须全绿（硬门禁）

- C1/C2 新增专项测试
- `tests/test_runtime_config_v2.py`
- `tests/test_config_v2.py`
- `tests/test_file_registry_bridge.py`
- `tests/test_config_lock_race.py`
- **`tests/test_hermes_cli_e2e.py`（单独跑，6 passed）**
- D 组 2 条（登记路径后应转绿）

### 允许豁免（且仅限这 10 条）

B 组 9 条 + C 组 1 条。豁免理由必须引用本文件第 3、4 节。

### 严禁

- 严禁修改 A 组任何测试文件或 `tests/hermes_e2e_helpers.py`
- 严禁把任何新出现的失败归入豁免清单
- 严禁用 `skip` / `xfail` 掩盖失败
- 严禁修改 B / C 组测试使其变绿（那是另案，不在本次范围）
- 出现清单外的失败 → **停手报告**

---

## 7. 排查过程中的方法教训

本轮排查推翻了三次自己的错误结论，成因均相同：**看到片段输出就下结论，未做对照实验**。

| 错误结论 | 实际 | 成因 |
|---|---|---|
| 未登记的 `sk-` 会被拦截 | 原样通过，零保护 | 把自己打印时截断的 `sk-pro...EyPl` 当成插件脱敏 |
| 私钥会阻断整个请求 | 0.4.4 是替换不阻断 | 拿 0.4.2 的行为当 0.4.4 现状 |
| A 组是 Hermes 连 GitHub 的环境问题 | 测试间污染，单跑全绿 | 未做单跑对照 |

**纪律**：

1. 判定脱敏是否生效 → 用布尔比对（`secret in payload`），不肉眼看截断字符串
2. 判定测试是否真红 → 必须单独跑一次对照
3. 判定行为归属版本 → 必须在目标版本上实测，不引用历史结论
4. 退出码绝不接管道（`| tail` 拿到的是 tail 的 0）
